/**
 * Margins for a page type (first page or continuation pages), in millimeters.
 */
export interface PageMargins {
  /** Top margin in mm */
  top: number;
  /** Bottom margin in mm */
  bottom: number;
  /** Left margin in mm */
  left: number;
  /** Right margin in mm */
  right: number;
}
