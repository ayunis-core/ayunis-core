export enum SourceType {
  TEXT = 'text',
  DATA = 'data',
}

export enum TextType {
  FILE = 'file',
  WEB = 'web',
}

export enum FileType {
  PDF = 'pdf',
}

export enum DataType {
  CSV = 'csv',
}
