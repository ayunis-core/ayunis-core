/**
 * Query for listing all available predefined MCP integration configurations.
 * This query has no fields as it returns public registry information
 * and does not require authentication.
 */
export class ListPredefinedMcpIntegrationConfigsQuery {
  constructor() {}
}
