packages:
  - "packages/*"
  - "ayunis-core-backend"
  - "ayunis-core-frontend"

# pnpm 10 does not run dependency build scripts unless allowlisted here.
# Keep in sync with the "Ignored build scripts" notice from `pnpm install`.
onlyBuiltDependencies:
  - "@appsignal/nodejs"
  - bcrypt
  - esbuild
